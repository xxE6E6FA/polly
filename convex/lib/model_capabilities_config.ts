// Re-export from shared configuration for backward compatibility
// This ensures existing Convex code continues to work while using the shared config

export * from "../../shared/model-capabilities-config";
