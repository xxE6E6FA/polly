import { NotFoundPage } from "@/components/ui/not-found-page";

export default function NotFound() {
  return (
    <NotFoundPage
      title="Conversation not found"
      description="This shared conversation doesn't exist or may have been removed."
    />
  );
}
