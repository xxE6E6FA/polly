// Placeholder initializer to set up middlewares/persistence later
// Keeping this as a no-op for now to establish the pattern.
export function useInitChatInputStore() {
  // In the future, we can wire zustand/persist here or hydrate from storage.
}
