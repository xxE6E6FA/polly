VITE_CONVEX_URL=
VITE_CLERK_PUBLISHABLE_KEY=

# Anonymous auth (Convex dashboard env vars, not Vite)
# ANON_AUTH_PRIVATE_KEY=   # RSA private key (PEM PKCS8) — set in Convex dashboard
# ANON_AUTH_PUBLIC_KEY=    # RSA public key (PEM SPKI) — set in Convex dashboard
# ANON_AUTH_ISSUER=        # e.g. https://<deployment>.convex.site — set in Convex dashboard
